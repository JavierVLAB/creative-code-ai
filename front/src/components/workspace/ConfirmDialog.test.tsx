import { describe, it, expect } from 'vitest'
import { ConfirmDialog } from './ConfirmDialog'

describe('ConfirmDialog', () => {
  it('es un componente funcional que se puede importar', () => {
    expect(typeof ConfirmDialog).toBe('function')
  })
})
