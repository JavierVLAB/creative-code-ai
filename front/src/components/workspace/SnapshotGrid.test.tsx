import { describe, it, expect } from 'vitest'
import { SnapshotGrid } from './SnapshotGrid'

describe('SnapshotGrid', () => {
  it('es un componente funcional que se puede importar', () => {
    expect(typeof SnapshotGrid).toBe('function')
  })
})
