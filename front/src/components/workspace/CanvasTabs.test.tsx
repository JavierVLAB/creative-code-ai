import { describe, it, expect } from 'vitest'
import { CanvasTabs } from './CanvasTabs'

describe('CanvasTabs', () => {
  it('es un componente funcional que se puede importar', () => {
    expect(typeof CanvasTabs).toBe('function')
  })
})
