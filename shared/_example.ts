// Ejemplo de tipo compartido entre frontend y backend.
// Documentación: los tipos comunes van en shared/, sin lógica ni imports de frameworks.
// Este archivo sirve como referencia para añadir nuevos tipos compartidos.

export interface ExampleSharedType {
  id: string
  name: string
}
